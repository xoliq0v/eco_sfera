# melos_managed_dependency_overrides: alice,database,firebase_eco,network
dependency_overrides:
  alice:
    path: ../alice
  database:
    path: ../../data/database
  firebase_eco:
    path: ../../data/firebase_eco
  network:
    path: ../../data/network
