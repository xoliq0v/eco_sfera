# melos_managed_dependency_overrides: alice,core,database,firebase_eco,network
dependency_overrides:
  alice:
    path: ../alice
  core:
    path: ../core
  database:
    path: ../../data/database
  firebase_eco:
    path: ../../data/firebase_eco
  network:
    path: ../../data/network
